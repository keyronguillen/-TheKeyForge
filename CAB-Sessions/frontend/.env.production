# Loaded automatically by `vite build` (production mode).
# Bakes the backend URL into the bundle so the deployed SPA calls Render directly.
# Not a secret — it's a public API URL — so it's safe to commit for this POC.
VITE_API_URL=https://thekeyforge.onrender.com

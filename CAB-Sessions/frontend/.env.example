# Base URL of the backend API.
# Local dev: leave blank (Vite proxies /api and /socket.io to localhost:4000).
# Production (Vercel): set to your Render backend URL, no trailing slash.
VITE_API_URL=https://thekeyforge.onrender.com
